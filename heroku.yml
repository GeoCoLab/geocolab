setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
    - plan: cloudamqp
      as: BROKER
    - plan: heroku-redis
      as: RESULTS
build:
  docker:
    web: deploy/nginx/Dockerfile
    frontend: deploy/frontend/Dockerfile
    backend: deploy/backend/Dockerfile
    tasks: deploy/celery/Dockerfile
run:
  api: gunicorn -b 0.0.0.0:5000 -w 4 backend.wsgi:app
